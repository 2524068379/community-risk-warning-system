version: 2

updates:
  # npm: 每天检查，每次只推一个 PR
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'daily'
      time: '09:00'
      timezone: 'Asia/Shanghai'
    rebase-strategy: auto
    open-pull-requests-limit: 1
    cooldown:
      semver-major-days: 7
      semver-minor-days: 1
      semver-patch-days: 1
    commit-message:
      prefix: 'chore'
      prefix-development: 'chore(dev)'
      include: 'scope'

  # GitHub Actions: 每天检查，每次只推一个 PR
  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'daily'
      time: '09:30'
      timezone: 'Asia/Shanghai'
    rebase-strategy: auto
    open-pull-requests-limit: 1
    cooldown:
      default-days: 1
    commit-message:
      prefix: 'ci'
      include: 'scope'
